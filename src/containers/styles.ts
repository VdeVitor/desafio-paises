import styled from 'styled-components';
import { symbols } from '../themes/symbols';

export const FilterButtonRow = styled.div`
  margin-bottom: ${symbols.spacing._32};
`;

export const ContentLoaderContainer = styled.div`
  margin-top: ${symbols.spacing._52};
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
`;
